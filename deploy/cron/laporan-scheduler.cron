# Laravel scheduler — jalankan tiap menit.
# Pasang dengan: crontab -u www-data -e  (lalu tempel baris di bawah)
* * * * * cd /var/www/laporan && /usr/bin/php85 artisan schedule:run >> /dev/null 2>&1
